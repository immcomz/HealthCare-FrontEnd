version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

    commands:
      - echo Installing source NPM dependencies...

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - echo Build started on `date`

  post_build:
    commands:
      - npm run build
